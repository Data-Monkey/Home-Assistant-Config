# defining what to log and where to log it too


  
influxdb:
  host: !secret INFLUX_HOST_IP
  username: !secret INFLUX_USER
  password: !secret INFLUX_PWD
  include:
    domains:
       - binary_sensor
       - sensor
       - switch

